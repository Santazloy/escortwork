# Balance Bot - Инструкция по настройке

## Описание

Balance Bot - это отдельный модуль для учета баланса в двух Telegram группах. Он **полностью независим** от основного функционала отправки анкет и не влияет на работу сервера.

### Функционал

- ✅ Отслеживает сообщения формата `+1000` или `-5000` в двух группах
- ✅ Ведет отдельный баланс для каждой группы
- ✅ Сохраняет всю историю транзакций в Supabase
- ✅ Поддерживает отрицательные балансы
- ✅ Форматирует сообщения на русском и китайском языках
- ✅ Сохраняет данные после рестарта/редеплоя

### Группы

- **Группа 1** (ID: `-1002774266933`) - Русский язык
- **Группа 2** (ID: `-1002468561827`) - Китайский язык

---

## Шаг 1: Создание нового бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Укажите имя бота (например: `MyBalanceBot`)
4. Укажите username (например: `my_balance_bot`)
5. **Сохраните токен** - он понадобится позже

**ВАЖНО:** Используйте ДРУГОГО бота, не того, который используется для отправки анкет!

---

## Шаг 2: Добавление бота в группы

1. Добавьте бота в обе группы:
   - Группа 1 (ID: `-1002774266933`)
   - Группа 2 (ID: `-1002468561827`)

2. Дайте боту права администратора в обеих группах с разрешениями:
   - ✅ Чтение сообщений
   - ✅ Отправка сообщений

3. Отключите Privacy Mode для бота через @BotFather:
   ```
   /mybots → выберите бота → Bot Settings → Group Privacy → Turn OFF
   ```

---

## Шаг 3: Настройка Supabase

### 3.1 Получение учетных данных

1. Войдите в свой проект на [Supabase](https://supabase.com)
2. Перейдите в `Settings` → `API`
3. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **API Key (anon/public)** - это публичный ключ

### 3.2 Создание таблиц

1. Откройте `SQL Editor` в Supabase
2. Скопируйте содержимое файла `supabase_schema.sql`
3. Вставьте в SQL Editor и нажмите `Run`

Будут созданы:
- Таблица `group_balances` - текущие балансы групп
- Таблица `balance_transactions` - история всех операций
- Индексы для быстрого поиска
- Триггеры для автообновления

### 3.3 Проверка таблиц

В разделе `Table Editor` вы должны увидеть:
- `group_balances` - с двумя записями (по одной на группу)
- `balance_transactions` - пока пустая

---

## Шаг 4: Настройка переменных окружения

Создайте файл `.env` в корне проекта (или обновите существующий):

```bash
# Существующие переменные (НЕ ТРОГАТЬ!)
BOT_TOKEN=7955217901:AAFJeZqMOvO5MY8-hsLg_jvSb8_iY7sEgZY
GROUP_ID=-1003244054167

# Новые переменные для Balance Bot
BALANCE_BOT_TOKEN=ваш_токен_от_BotFather
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=ваш_публичный_ключ_supabase
```

**ВАЖНО:**
- `BALANCE_BOT_TOKEN` - это токен НОВОГО бота, не основного
- Не удаляйте и не изменяйте переменные `BOT_TOKEN` и `GROUP_ID`

---

## Шаг 5: Запуск Balance Bot

### Linux/Mac:
```bash
./start_balance_bot.sh
```

### Windows:
```batch
start_balance_bot.bat
```

### Через Python напрямую:
```bash
# Активируйте виртуальное окружение
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate.bat  # Windows

# Запустите бота
python3 balance_bot.py
```

---

## Использование

### Формат команд

В любой из двух групп просто пишите:

- `+1000` - добавить 1000 к балансу
- `-500` - вычесть 500 из баланса
- `+2500.50` - можно с копейками
- `+1,000` - можно с запятыми (они будут удалены)

### Примеры ответов

**Русская группа (пополнение):**
```
💰 Баланс пополнен!
📈 Было: 56,697 ¥
➕ Пополнение: 3,300 ¥
💎 Итоговый баланс: 59,997 ¥
```

**Русская группа (списание):**
```
💸 Списание с баланса
📈 Было: 59,997 ¥
➖ Списание: 4,999 ¥
💎 Итоговый баланс: 54,998 ¥
```

**Китайская группа (пополнение):**
```
💰 余额已充值！
📈 之前余额: 56,697 ¥
➕ 充值金额: 3,300 ¥
💎 当前余额: 59,997 ¥
```

**Китайская группа (списание):**
```
💸 余额已扣除
📈 之前余额: 59,997 ¥
➖ 扣除金额: 4,999 ¥
💎 当前余额: 54,998 ¥
```

---

## Архитектура

### Файловая структура

```
escortwork/
├── server.py              # Основной сервер (НЕ ТРОНУТ)
├── bot.py                 # Бот для анкет (НЕ ТРОНУТ)
├── balance_bot.py         # ⭐ НОВЫЙ: Бот для учета баланса
├── supabase_schema.sql    # ⭐ НОВЫЙ: SQL схема для Supabase
├── start_balance_bot.sh   # ⭐ НОВЫЙ: Запуск для Unix
├── start_balance_bot.bat  # ⭐ НОВЫЙ: Запуск для Windows
├── requirements.txt       # ОБНОВЛЕН: добавлен supabase==2.3.4
└── BALANCE_BOT_SETUP.md   # ⭐ НОВЫЙ: Эта инструкция
```

### База данных Supabase

**Таблица `group_balances`:**
```sql
id              | BIGSERIAL (PK)
group_id        | BIGINT (UNIQUE) - ID группы Telegram
group_name      | VARCHAR - название группы
current_balance | DECIMAL(15,2) - текущий баланс
language        | VARCHAR - язык (ru/zh)
created_at      | TIMESTAMP
updated_at      | TIMESTAMP
```

**Таблица `balance_transactions`:**
```sql
id               | BIGSERIAL (PK)
group_id         | BIGINT (FK) - ссылка на group_balances
user_id          | BIGINT - ID пользователя Telegram
username         | VARCHAR - username пользователя
amount           | DECIMAL(15,2) - сумма операции (+/-)
previous_balance | DECIMAL(15,2) - баланс до операции
new_balance      | DECIMAL(15,2) - баланс после операции
transaction_type | VARCHAR - 'add' или 'subtract'
message_id       | BIGINT - ID сообщения Telegram
created_at       | TIMESTAMP
```

---

## Проверка работоспособности

### 1. Проверка подключения к Supabase

Запустите Python и выполните:

```python
import os
from supabase import create_client

SUPABASE_URL = "ваш_url"
SUPABASE_KEY = "ваш_ключ"

supabase = create_client(SUPABASE_URL, SUPABASE_KEY)
response = supabase.table('group_balances').select('*').execute()
print(response.data)
```

Должны увидеть две записи для групп.

### 2. Проверка бота

1. Запустите `balance_bot.py`
2. В логах должно появиться:
   ```
   Balance bot запущен и готов к работе!
   Отслеживаемые группы: -1002774266933 (RU), -1002468561827 (ZH)
   ```
3. Напишите в одну из групп: `+100`
4. Бот должен ответить форматированным сообщением

### 3. Проверка базы данных

После нескольких транзакций проверьте в Supabase:

```sql
-- Посмотреть текущие балансы
SELECT * FROM group_balances;

-- Посмотреть последние 10 транзакций
SELECT * FROM balance_transactions
ORDER BY created_at DESC
LIMIT 10;
```

---

## Деплой на продакшн (Render/Heroku)

### Переменные окружения на сервере

Добавьте в настройки деплоя:

```
BALANCE_BOT_TOKEN=ваш_токен
SUPABASE_URL=ваш_url
SUPABASE_KEY=ваш_ключ
```

### Два отдельных процесса

Вам нужно запустить **два независимых процесса**:

1. **Web-сервер** (существующий):
   ```bash
   gunicorn server:app
   ```

2. **Balance Bot** (новый):
   ```bash
   python3 balance_bot.py
   ```

**На Render:**
- Создайте два отдельных сервиса
- Или используйте `Procfile` для запуска обоих

**Пример Procfile:**
```
web: gunicorn server:app
worker: python3 balance_bot.py
```

---

## Безопасность

### Что защищено:
✅ Использование переменных окружения
✅ Отдельные токены для разных ботов
✅ Сохранение всех транзакций в БД
✅ Независимость от основного функционала

### Рекомендации:
1. **Никогда** не коммитьте `.env` файл в git
2. Используйте разные токены для dev/prod
3. Регулярно проверяйте логи на ошибки
4. Создайте бэкап базы данных

---

## Решение проблем

### Бот не отвечает на сообщения

**Причины:**
1. Privacy Mode включен в настройках бота
2. Бот не является администратором группы
3. Неправильный ID группы

**Решение:**
- Проверьте настройки через @BotFather
- Дайте боту права администратора
- Проверьте логи бота

### Ошибка подключения к Supabase

**Причины:**
1. Неправильный URL или ключ
2. Таблицы не созданы
3. Firewall блокирует соединение

**Решение:**
- Проверьте переменные окружения
- Запустите `supabase_schema.sql`
- Проверьте сетевое подключение

### Баланс не сохраняется после рестарта

**Причины:**
1. Данные не записываются в Supabase
2. Используется другая база данных

**Решение:**
- Проверьте логи на ошибки записи
- Убедитесь что `SUPABASE_URL` и `SUPABASE_KEY` правильные
- Проверьте данные в Supabase Table Editor

---

## FAQ

**Q: Можно ли добавить больше групп?**
A: Да, добавьте ID новых групп в код `balance_bot.py` в переменные `GROUP_RU` / `GROUP_ZH` и добавьте записи в `group_balances`.

**Q: Можно ли изменить формат сообщений?**
A: Да, отредактируйте функции `format_message_ru()` и `format_message_zh()` в `balance_bot.py`.

**Q: Как посмотреть историю транзакций?**
A: Зайдите в Supabase → Table Editor → `balance_transactions` или напишите SQL запрос.

**Q: Влияет ли Balance Bot на работу основного сервера?**
A: Нет, это полностью независимый модуль. Даже если Balance Bot упадет, основной сервер продолжит работать.

**Q: Можно ли запустить оба бота на одном токене?**
A: Технически да, но **не рекомендуется**. Лучше использовать разные токены для разных функций.

---

## Поддержка

При возникновении проблем:
1. Проверьте логи: `tail -f balance_bot.log`
2. Проверьте переменные окружения: `echo $BALANCE_BOT_TOKEN`
3. Проверьте Supabase Dashboard на наличие таблиц
4. Убедитесь что бот является админом обеих групп

---

## Changelog

**v1.0.0 (2025-11-10)**
- ✨ Начальный релиз
- ✅ Поддержка двух групп
- ✅ Русский и китайский языки
- ✅ Интеграция с Supabase
- ✅ История транзакций